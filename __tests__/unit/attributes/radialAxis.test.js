import RadialAxis from 'c/radialAxis';
import * as Handler from '../../__utils__/testAttributeHandler.js';

const TEST_DATA_PROPERTIES = [
  //new Handler.ChartOptionMock('text', 'foo', { display: true, text: 'foo' })
];

describe('c-radial-axis', () => {
  //Handler.testAttribute(RadialAxis, TEST_DATA_PROPERTIES);
});

// Remove after test is implemented
test('', () => {});
